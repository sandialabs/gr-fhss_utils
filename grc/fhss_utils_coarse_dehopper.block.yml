# auto-generated by grc.converter

id: fhss_utils_coarse_dehopper
label: Coarse Dehopper
category: '[Sandia]/FHSS Utilities'

parameters:
-   id: fft_len
    label: FFT Length
    dtype: raw
    default: '32'
    hide: none
-   id: freq_sample_delay_samps
    label: Freq S&H Delay
    dtype: raw
    default: '128'
    hide: none
-   id: freq_samps_to_avg
    label: Freq Avg Samples
    dtype: raw
    default: '128'
    hide: none
-   id: mag_samps_to_avg
    label: Mag Avg Samples
    dtype: raw
    default: '128'
    hide: none
-   id: thresh
    label: Threshold
    dtype: raw
    default: '0.005'
    hide: none

inputs:
-   domain: stream
    dtype: complex
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: import fhss_utils
    make: fhss_utils.coarse_dehopper(${fft_len}, ${freq_sample_delay_samps}, ${freq_samps_to_avg},
        ${mag_samps_to_avg}, ${thresh})
    callbacks:
    - set_fft_len(${fft_len})
    - set_freq_sample_delay_samps(${freq_sample_delay_samps})
    - set_freq_samps_to_avg(${freq_samps_to_avg})
    - set_mag_samps_to_avg(${mag_samps_to_avg})
    - set_thresh(${thresh})

file_format: 1
