# auto-generated by grc.converter

id: fhss_utils_fine_burst_measure
label: Fine Burst Measure
category: '[Sandia]/FHSS Utilities'

parameters:
-   id: freq_resolution
    label: Frequency Resolution
    dtype: float
    default: '4000'
-   id: samples
    label: Analysis Samples
    dtype: int
    default: '2048'
-   id: threshold
    label: Threshold
    dtype: float
    default: '0.5'

inputs:
-   domain: message
    id: pdu_in

outputs:
-   domain: message
    id: pdu_out

templates:
    imports: import fhss_utils
    make: fhss_utils.fine_burst_measure(${freq_resolution}, ${samples}, ${threshold})

documentation: |-
    Estimates burst metrics.

file_format: 1
