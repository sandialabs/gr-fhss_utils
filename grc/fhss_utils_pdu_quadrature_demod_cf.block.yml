# auto-generated by grc.converter

id: fhss_utils_pdu_quadrature_demod_cf
label: PDU Quadrature Demod
category: '[Sandia]/FHSS Utilities'

parameters:
-   id: sensitivity
    label: Sensitivity
    dtype: float
-   id: taps
    label: Input Taps
    dtype: real_vector

inputs:
-   domain: message
    id: cpdus

outputs:
-   domain: message
    id: fpdus

templates:
    imports: import fhss_utils
    make: fhss_utils.pdu_quadrature_demod_cf(${taps}, ${sensitivity})

file_format: 1
