# auto-generated by grc.converter

id: fhss_utils_coerce_frequency_estimate
label: Coerce Frequency
category: '[Sandia]/FHSS Utilities'

parameters:
-   id: valid_freqs
    label: Valid Freqs
    dtype: float_vector

inputs:
-   domain: message
    id: cpdus

outputs:
-   domain: message
    id: cpdus
    optional: true

templates:
    imports: import fhss_utils
    make: fhss_utils.coerce_frequency_estimate(${valid_freqs})
    callbacks:
    - set_freqs(${valid_freqs})

documentation: |-
    This block will coerce the frequency estimate to the nearest one in a provided list.

file_format: 1
