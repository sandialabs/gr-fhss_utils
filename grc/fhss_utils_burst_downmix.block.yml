# auto-generated by grc.converter

id: fhss_utils_burst_downmix
label: Burst Downmix
category: '[Sandia]/FHSS Utilities'

parameters:
-   id: decimation
    label: Decimation
    dtype: int
-   id: taps
    label: Taps
    dtype: real_vector

inputs:
-   domain: message
    id: cpdus

outputs:
-   domain: message
    id: cpdus
    optional: true

templates:
    imports: import fhss_utils
    make: fhss_utils.burst_downmix(${taps}, ${decimation})

file_format: 1
